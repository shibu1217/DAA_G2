//stl sort(),all_of(),any_of(),_none_of(),copy_n()

#include <iostream>

#include<algorithm>


using namespace std;


void showArray(int arr[],int n)
{
    
for(int i=0;i<n;i++)
    {
       
 cout<<arr[i]<<" ";
    }
}


int main()
{
 

  int arr[]={-4,-2,-6,-1,-7,-3,-9,-5,0};
   
int size=sizeof(arr)/sizeof(arr[0]);
  
 cout<<"Array without sorting:"<<endl;
   
showArray(arr,size);
  
 sort(arr,arr+size);
   
cout<<"\n Array with sorting:"<<endl;
 
  showArray(arr,size);
   
if(binary_search(arr,arr+size,99))
   {
     
  cout<<"\nValue found";
   }
   
else {
     
  cout<<"\n ValueNOT Found";
   }
  
 if(none_of(arr,arr+size,[](int x){return x>0;}))
   cout<<"\nall are negative "<<endl;
 
  else
    cout<<"\n some are positive"<<endl;
    showArray(arr,size);
   
 int arr2[size];
  
  copy_n(arr,size,arr2);
   
 cout<<"\nCopied array is:\n";
   
 //for(int i=0;i<arr2.size;i++)
    
cout<<arr2[n]<<" ";
   
return 0;
}